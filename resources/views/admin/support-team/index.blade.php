<x-admin-layout page-heading="Support team" :navigation="[]"><div class="admin-page"><div class="admin-page-inner space-y-6">@if($credentials)<x-admin.panel><p class="admin-eyebrow">Activation credentials</p><p class="admin-panel-copy">Temporary passwords are shown once and cannot be viewed again. Support staff must choose a new password on first sign-in.</p><dl class="mt-4 space-y-2 text-sm"><div><dt class="font-semibold">Email</dt><dd>{{ $credentials['email'] }}</dd></div><div><dt class="font-semibold">Temporary password</dt><dd class="font-mono">{{ $credentials['password'] }}</dd></div><div><dt class="font-semibold">Activation link</dt><dd class="break-all"><a class="admin-inline-link" href="{{ $credentials['activation_url'] }}">{{ $credentials['activation_url'] }}</a></dd></div><div><dt class="font-semibold">Expires</dt><dd>{{ $credentials['expires_at'] }}</dd></div></dl></x-admin.panel>@endif<x-admin.panel><form method="POST" action="{{ route('admin.support-team.store') }}" class="grid gap-4 md:grid-cols-3">@csrf<div><label class="admin-label">Name</label><input name="name" class="admin-control" required></div><div><label class="admin-label">Email</label><input name="email" type="email" class="admin-control" required></div><div><label class="admin-label">Phone</label><input name="phone" class="admin-control"></div><div class="md:col-span-3"><button class="admin-button">Create support staff</button></div></form></x-admin.panel><x-admin.panel><div class="overflow-x-auto"><table class="min-w-full"><thead class="admin-table-head"><tr><th class="admin-table-head-cell">Name</th><th class="admin-table-head-cell">Status</th><th class="admin-table-head-cell">Activation</th><th class="admin-table-head-cell">Last login</th><th class="admin-table-head-cell">Actions</th></tr></thead><tbody class="admin-table-body">@forelse($members as $member)<tr><td class="px-4 py-4"><p class="font-medium">{{ $member->name }}</p><p class="text-xs text-slate-500">{{ $member->email }}</p></td><td class="px-4 py-4">{{ $member->suspended_at ? 'Suspended' : 'Active' }}</td><td class="px-4 py-4">{{ $member->activated_at ? 'Activated' : 'Awaiting activation' }}</td><td class="px-4 py-4">{{ $member->last_login_at?->diffForHumans() ?? 'Never' }}</td><td class="px-4 py-4"><div class="flex gap-3">@if($member->suspended_at)<form method="POST" action="{{ route('admin.support-team.reactivate',$member) }}">@csrf<button class="admin-inline-link">Reactivate</button></form>@else<form method="POST" action="{{ route('admin.support-team.suspend',$member) }}">@csrf<button class="admin-inline-link">Suspend</button></form>@endif@if(!$member->activated_at)<form method="POST" action="{{ route('admin.support-team.reissue',$member) }}">@csrf<button class="admin-inline-link">Reissue activation</button></form>@endif</div></td></tr>@empty<tr><td colspan="5" class="px-4 py-8">No support staff yet.</td></tr>@endforelse</tbody></table></div></x-admin.panel></div></div></x-admin-layout>
