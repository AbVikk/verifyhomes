<div class="admin-page"><div class="admin-page-inner space-y-6"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="admin-eyebrow">Help and support</p><h2 class="admin-panel-title">Your support requests</h2><p class="admin-panel-copy">Keep inspection, payment, maintenance, and account questions in one conversation.</p></div><a href="{{ $createRoute }}" class="admin-button">Submit a support request</a></div><x-admin.panel><div class="overflow-x-auto"><table class="min-w-full"><thead class="admin-table-head"><tr><th class="admin-table-head-cell">Reference</th><th class="admin-table-head-cell">Request</th><th class="admin-table-head-cell">Status</th><th class="admin-table-head-cell">Updated</th><th class="admin-table-head-cell"><span class="sr-only">Action</span></th></tr></thead><tbody class="admin-table-body">@forelse($requests as $request)<tr><td class="px-4 py-4 text-sm font-mono">{{ $request->reference }}</td><td class="px-4 py-4"><a href="{{ route($showRoutePrefix, $request) }}" class="font-medium text-slate-900">{{ $request->subject }}</a><p class="mt-1 text-xs text-slate-500">{{ $request->categoryLabel() }}</p></td><td class="px-4 py-4"><span class="admin-badge admin-badge-neutral">{{ str($request->status)->replace('_',' ')->headline() }}</span></td><td class="px-4 py-4 text-sm text-slate-600">{{ $request->updated_at->diffForHumans() }}</td><td class="px-4 py-4 text-sm"><a href="{{ route($showRoutePrefix, $request) }}" class="admin-inline-link">{{ in_array($request->status, ['open', 'in_progress', 'waiting_for_user'], true) ? 'Continue conversation' : 'View request' }}</a></td></tr>@empty<tr><td colspan="5" class="px-4 py-10"><x-admin.empty-state title="You haven't contacted support yet." copy="Send a request when you need help with your VerifyHomes account or activity." /></td></tr>@endforelse</tbody></table></div></x-admin.panel></div></div>
