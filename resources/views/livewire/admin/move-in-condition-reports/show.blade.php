<div class="admin-page"><div class="admin-page-inner space-y-6"><x-admin.panel><div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p class="admin-eyebrow">Evidence review</p><h2 class="admin-panel-title">Move-in condition report</h2><p class="admin-panel-copy">Read-only operational record for {{ $report->property?->title }}.</p></div><button type="button" onclick="window.print()" class="admin-button admin-button-secondary print:hidden">Print report</button></div></x-admin.panel><x-admin.panel><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p class="text-sm text-slate-500">Tenant</p><p>{{ $report->tenant?->name }}</p></div><div><p class="text-sm text-slate-500">Landlord</p><p>{{ $report->landlord?->name }}</p></div><div><p class="text-sm text-slate-500">Status</p><p>{{ str($report->status)->headline() }}</p></div><div><p class="text-sm text-slate-500">Submitted</p><p>{{ $report->submitted_at?->format('M j, Y') ?? 'Draft' }}</p></div></div>@if($report->tenant_notes)<div class="admin-alert admin-alert-warning mt-4">Tenant note: {{ $report->tenant_notes }}</div>@endif</x-admin.panel><div class="space-y-6">@foreach($report->items->groupBy('category') as $category=>$items)<x-admin.panel><h3 class="text-lg font-semibold">{{ $category }}</h3><div class="mt-4 grid gap-3 md:grid-cols-2">@foreach($items as $item)<div class="admin-subsurface p-4"><p class="font-medium">{{ $item->label }}</p><p class="text-sm text-slate-600">{{ $item->rating ? str($item->rating)->replace('_',' ')->headline() : 'Not recorded' }}</p>@if($item->notes)<p class="mt-2 text-sm">{{ $item->notes }}</p>@endif</div>@endforeach</div></x-admin.panel>@endforeach</div><x-admin.panel><h3 class="text-lg font-semibold">Evidence</h3><div class="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4">@foreach($report->photos as $photo)<a href="{{ route('admin.move-in-condition-photos.show',$photo) }}" target="_blank"><img src="{{ route('admin.move-in-condition-photos.show',$photo) }}" alt="{{ str($photo->source)->headline() }} evidence" class="aspect-square w-full rounded-xl object-cover"></a>@endforeach</div></x-admin.panel></div></div>
