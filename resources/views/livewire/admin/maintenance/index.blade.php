<div class="admin-page"><div class="admin-page-inner"><x-admin.panel><div class="space-y-3">@foreach($requests as $request)<div class="admin-data-box flex flex-col gap-2 sm:flex-row sm:justify-between"><div><p class="font-medium">{{$request->title}}</p><p class="text-sm text-slate-600">{{$request->property?->title}} · {{$request->tenant?->name}} · {{$request->landlord?->name}} · {{str($request->category)->headline()}}</p></div><div class="flex gap-2"><x-status-chip tone="{{$request->priority==='urgent'||$request->escalated_at?'warning':'info'}}">{{str($request->status)->headline()}}{{ $request->escalated_at ? ' · Escalated' : ''}}</x-status-chip><a class="admin-inline-link" href="{{route('admin.maintenance.show',$request)}}">View</a></div></div>@endforeach</div></x-admin.panel></div></div>
