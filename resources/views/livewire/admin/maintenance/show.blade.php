<div class="admin-page"><div class="admin-page-inner space-y-6"><x-admin.panel><h2 class="admin-panel-title">{{$request->title}}</h2><p class="admin-panel-copy">{{$request->property?->title}} · {{$request->tenant?->name}} · {{$request->landlord?->name}}</p><p class="mt-4">{{$request->description}}</p></x-admin.panel><x-admin.panel><h3 class="admin-panel-title">Evidence</h3><div class="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-4">@foreach($request->photos as $photo)<a target="_blank" href="{{route('admin.maintenance-photos.show',$photo)}}"><img src="{{route('admin.maintenance-photos.show',$photo)}}" alt="{{str($photo->source)->headline()}} evidence" class="aspect-square rounded-xl object-cover"></a>@endforeach</div></x-admin.panel><x-admin.panel><h3 class="admin-panel-title">Timeline</h3>@foreach($request->events as $event)<p class="mt-2 text-sm">{{str($event->event_type)->headline()}} · {{$event->created_at->format('M j, Y g:i A')}} @if($event->note) — {{$event->note}} @endif</p>@endforeach</x-admin.panel></div></div>
