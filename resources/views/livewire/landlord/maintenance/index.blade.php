<div class="admin-page"><div class="admin-page-inner space-y-6"><x-admin.panel><p class="admin-eyebrow">Maintenance</p><h2 class="admin-panel-title">Property maintenance requests</h2></x-admin.panel><x-admin.panel><div class="space-y-3">@forelse($requests as $request)<div class="admin-data-box flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p class="font-medium">{{$request->title}}</p><p class="text-sm text-slate-600">{{$request->property?->title}} · {{$request->tenant?->name}} · {{str($request->category)->headline()}} · {{str($request->priority)->headline()}}</p><p class="text-xs text-slate-500">{{$request->created_at->diffForHumans()}}</p></div><div class="flex items-center gap-3"><x-status-chip tone="{{$request->priority==='urgent'?'warning':'info'}}">{{str($request->status)->headline()}}</x-status-chip><a class="admin-inline-link" href="{{route('landlord.maintenance.show',$request)}}">View request</a></div></div>@empty<p class="text-slate-600">No maintenance requests yet.</p>@endforelse</div></x-admin.panel></div></div>
